import { Component } from '@angular/core';

@Component({
    selector: 'to-do-list',
    template: `
        <ng-container #vc></ng-container>
    `,
})
export class TodoListComponent {

    addItem(v) {
    }

    removeItem(index) {
    }
}
